<main class="thanks-main">
 <nav>
      <h1><%= image_tag "logo.png" %> <%= link_to "Coast Car", root_path %></h1>
      <ul>
       <% if current_user %>
        <li class="username">
            Welcome, <%= current_user.name %>!
        </li>
        <% end %>
        <li><%= link_to "Offer a ride", current_user ?
        offer_a_ride_path :
        register_path(user_type: :driver) %></li>
        <li><%= link_to "How it works", "#howdoesitwork" %></li>
        <% if current_user %>
        <li>
            <%= link_to "Sign Out", signout_path %>
        </li>
        <% end %>
      </ul>
    </nav>
  <section class="thanks">
    <div class="modal">
      <h1>Hey, thanks!</h1>
      <p>You're one of our first members, and that's really awesome. We are currently finishing up our beta and we'll be sure to notify you when Coast Car is ready for you!</p>
      <div class="thanks-home"><%= link_to "Home", root_path %></div>
    </div>
  </section>
</main>